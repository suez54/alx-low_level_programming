#include <stdio.h>

/**
 * mul - Multiples two integers.
 * @a: The first integer to be multiplied.
 * @b: The second integer to be multiplied.
 * Return: Answer.
 */
int mul(int a, int b)
{
return (a * b);
}
