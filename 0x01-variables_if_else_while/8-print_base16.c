#include<stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
int y;
int x;

for (x = 48; x <= 57; x++)
{
putchar(x);
}
for (y = 97; y <= 102; y++)
{
putchar(y);
}
putchar('\n');
return (0);
}
